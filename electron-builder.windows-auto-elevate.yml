extends: ./electron-builder.yml
extraMetadata:
  kokoroboxWindowsElevation: auto-elevate
win:
  requestedExecutionLevel: requireAdministrator
nsis:
  artifactName: ${name}-windows-${version}-${arch}-auto-elevate-setup.${ext}
