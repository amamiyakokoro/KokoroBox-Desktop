# Unsigned local smoke-test configuration only. Release Actions do not use this file.
# Official CI packages require scripts/macos-signing.ts and its verified notarization receipt.
extends: ./electron-builder.yml
mac:
  identity: null
  notarize: false
